import type { SaturationProps } from '../../../domain';

// Ce fichier declare le DTO de sortie de saturation.

/** Ce type represente une saturation applicative. */
export type SaturationDto = SaturationProps;
