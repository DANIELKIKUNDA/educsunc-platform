import type { RegisterSignalInputDto } from '../dto/input';

// Ce fichier declare la commande d enregistrement d un signal.

/** Cette interface represente la commande d enregistrement d un signal. */
export interface RegisterSignalCommand extends RegisterSignalInputDto {}
