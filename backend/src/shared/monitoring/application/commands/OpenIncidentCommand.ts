import type { OpenIncidentInputDto } from '../dto/input';

// Ce fichier declare la commande d ouverture d un incident.

/** Cette interface represente la commande d ouverture d un incident. */
export interface OpenIncidentCommand extends OpenIncidentInputDto {}
