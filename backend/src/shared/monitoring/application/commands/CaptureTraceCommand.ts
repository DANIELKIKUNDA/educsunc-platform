import type { CaptureTraceInputDto } from '../dto/input';

// Ce fichier declare la commande de capture d une trace.

/** Cette interface represente la commande de capture d une trace. */
export interface CaptureTraceCommand extends CaptureTraceInputDto {}
