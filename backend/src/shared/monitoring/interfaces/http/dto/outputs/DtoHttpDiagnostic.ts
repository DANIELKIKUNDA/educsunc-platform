import type { DiagnosticDto } from '../../../../application';

// Ce fichier declare le DTO HTTP de diagnostic.

/** Ce type represente la projection HTTP d un diagnostic. */
export type DtoHttpDiagnostic = DiagnosticDto;
