import type { HealthSnapshotDto } from '../../../../application';

// Ce fichier declare le DTO HTTP de snapshot de sante.

/** Ce type represente la projection HTTP de sante. */
export type DtoHttpHealthSnapshot = HealthSnapshotDto;
