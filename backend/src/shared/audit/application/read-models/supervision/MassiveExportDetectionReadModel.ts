// Ce read-model optimise une lecture applicative du BC Audit.
export interface MassiveExportDetectionReadModel { readonly exportId?: string; readonly totalLignes?: number; readonly gravite: string; }
