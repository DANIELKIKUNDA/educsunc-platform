import type { DtoRejeuNotification } from '../../../../application';

// Ce fichier expose le DTO HTTP d'entree pour le rejeu de notification.

/** Cette interface represente le body HTTP de rejeu de notification. */
export interface DtoHttpReplayNotification extends DtoRejeuNotification {}
