import type { DtoEscaladeNotification } from '../../../../application';

// Ce fichier expose le DTO HTTP d'entree pour l'escalade de notification.

/** Cette interface represente le body HTTP d'escalade. */
export interface DtoHttpEscaladeNotification extends DtoEscaladeNotification {}
