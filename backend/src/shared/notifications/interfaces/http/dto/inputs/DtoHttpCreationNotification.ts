import type { DtoCommandeCreationNotification } from '../../../../application';

// Ce fichier expose le DTO HTTP d'entree pour la creation de notification.

/** Cette interface represente le body HTTP de creation de notification. */
export interface DtoHttpCreationNotification extends DtoCommandeCreationNotification {}
