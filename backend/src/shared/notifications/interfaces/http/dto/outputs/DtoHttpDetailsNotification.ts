import type { DtoDetailsNotification } from '../../../../application';

// Ce fichier expose le DTO HTTP de sortie pour le detail d'une notification.

/** Cette interface represente la vue detail HTTP stable d'une notification. */
export interface DtoHttpDetailsNotification extends DtoDetailsNotification {}
