import type { DtoHttpValidationConfiguration } from '../dto/outputs';

// Ce fichier declare les contrats HTTP de validation Configuration.

export interface ContratHttpValidationConfiguration {
  readonly donnees: DtoHttpValidationConfiguration;
}
