import { TYPES_FEATURE_CONFIGURATION, TypeFeatureConfiguration } from '../enums';

// Ce fichier centralise les constantes des features configuration.

/** Cette constante expose la liste officielle des features configuration. */
export const FEATURES_CONFIGURATION: readonly TypeFeatureConfiguration[] = TYPES_FEATURE_CONFIGURATION;
