import { TYPES_MODULE_CONFIGURATION, TypeModuleConfiguration } from '../enums';

// Ce fichier centralise les constantes des modules configuration.

/** Cette constante expose la liste officielle des modules activables. */
export const MODULES_CONFIGURATION: readonly TypeModuleConfiguration[] = TYPES_MODULE_CONFIGURATION;
