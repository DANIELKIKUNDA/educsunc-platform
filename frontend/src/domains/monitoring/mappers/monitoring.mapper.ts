export function lireEnveloppe<T>(payload: unknown, fallback: T): T { if(payload&&typeof payload==='object'){const c=payload as Record<string,unknown>; if('donnees'in c)return (c.donnees as T)??fallback;if('data'in c)return(c.data as T)??fallback;} return (payload as T)??fallback; }
export function lireListe<T>(payload: unknown): readonly T[]{const data=lireEnveloppe<unknown>(payload,[]);return Array.isArray(data)?data as T[]:[];}
