<template><div class="mon-health-grid"><article v-for="item in items" :key="item.nom" class="mon-health-card"><div class="mon-health-card__head"><strong>{{ item.nom }}</strong><MonitoringStatusBadge :level="item.niveau"/></div><p>{{ item.message }}</p><dl><div><dt>Latence</dt><dd>{{ item.latenceMillisecondes == null ? '—' : `${item.latenceMillisecondes} ms` }}</dd></div><div><dt>Dernier controle</dt><dd>{{ formatDate(item.dernierControleLe) }}</dd></div></dl></article></div></template>
<script setup lang="ts">import type { ComponentHealth } from '../models/monitoring.model'; import MonitoringStatusBadge from './MonitoringStatusBadge.vue'; defineProps<{items:readonly ComponentHealth[]}>(); const formatDate=(v:string)=>{const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('fr-FR',{dateStyle:'short',timeStyle:'medium'}).format(d)};</script>
<style scoped>.mon-health-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:1rem}.mon-health-card{border:1px solid rgba(15,42,67,.1);border-radius:20px;padding:1rem;background:#fff}.mon-health-card__head{display:flex;align-items:center;justify-content:space-between;gap:.8rem}.mon-health-card p{color:#637386;min-height:2.4em}.mon-health-card dl{display:grid;grid-template-columns:1fr 1fr;gap:.7rem;margin:0}.mon-health-card dl div{background:#f7fafc;border-radius:14px;padding:.65rem}.mon-health-card dt{font-size:.72rem;color:#7b8998}.mon-health-card dd{margin:.15rem 0 0;font-weight:700;color:#24364b;font-size:.82rem}</style>
