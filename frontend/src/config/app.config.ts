import { configurationOffline } from './offline.config';

// Centralise la configuration globale du frontend.
export const configurationApplicationFrontend = {
  nom: 'EducSyn Frontend',
  modeParDefaut: configurationOffline.modeParDefaut,
};
